name: Build and Push Docker Image

on:
  push:
    branches: [ main, develop ]
    tags: [ 'v*' ]
    paths-ignore:
      - 'k8s/**'
      - '*.md'
      - 'docs/**'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - 'k8s/**'
      - '*.md'
      - 'docs/**'
  workflow_dispatch:

env:
  REGISTRY: harbor.sirddail.net
  INTERNAL_REGISTRY: harbor-core.harbor.svc.cluster.local
  PROJECT: stable-diffusion
  IMAGE_NAME: stable-diffution-webui-rcom

jobs:
  build:
    runs-on: stable-diffution-webui-rcom-runner
    permissions:
      contents: read

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Fetch secrets from Infisical
      uses: Infisical/secrets-action@v1.0.7
      with:
        client-id: ${{ secrets.INFISICAL_CLIENT_ID }}
        client-secret: ${{ secrets.INFISICAL_CLIENT_SECRET }}
        env-slug: prod
        project-slug: homelab-bz-gt
        secret-path: /
        export-type: env
        domain: http://infisical-infisical-standalone-infisical.infisical.svc.cluster.local:8080

    - name: Debug cert trust
      run: |
        echo "=== /etc/docker/certs.d ==="
        ls -la /etc/docker/certs.d/ 2>/dev/null || echo "directory not found"
        echo "=== harbor dir ==="
        ls -la /etc/docker/certs.d/harbor-core.harbor.svc.cluster.local/ 2>/dev/null || echo "harbor dir not found"
        echo "=== curl test ==="
        curl -v --cacert /etc/docker/certs.d/harbor-core.harbor.svc.cluster.local/ca.crt \
          https://harbor-core.harbor.svc.cluster.local/v2/ 2>&1 | grep -E "SSL|TLS|issuer|subject|< HTTP|curl:"

    - name: Log in to Harbor (external)
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ env.HB_USERNAME }}
        password: ${{ env.HB_PASSWORD }}

    - name: Log in to Harbor (internal)
      uses: docker/login-action@v3
      with:
        registry: ${{ env.INTERNAL_REGISTRY }}
        username: ${{ env.HB_USERNAME }}
        password: ${{ env.HB_PASSWORD }}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.PROJECT }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=sha,prefix={{branch}}-

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        build-args: |
          STABLE_DIFFUSION_TAG=v1.10.1
        cache-from: type=registry,ref=${{ env.INTERNAL_REGISTRY }}/${{ env.PROJECT }}/${{ env.IMAGE_NAME }}:buildcache
        cache-to: type=registry,ref=${{ env.INTERNAL_REGISTRY }}/${{ env.PROJECT }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

    - name: Test image
      if: github.event_name != 'pull_request'
      run: |
        docker run --rm ${{ env.REGISTRY }}/${{ env.PROJECT }}/${{ env.IMAGE_NAME }}:${{ github.sha }} --help || true
